# context-compaction

Validates compacted turn context plus synthetic continuation under a small token budget.

Covered lego planes: context builder, compaction policy, continuation policy, provider stream runner, trace recorder.
