# read-only-answer

Validates that a product harness can inspect a workspace file, preserve session/tool trace evidence, and return an equivalent user-visible answer without modifying the workspace.

Covered lego planes: prompt, turn, tool registry, permission policy, session persistence, trace recorder, product shell.
