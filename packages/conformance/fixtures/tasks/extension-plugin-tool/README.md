# extension-plugin-tool

Validates a dynamically available extension-style tool through the shared tool registry using the common echo tool.

Covered lego planes: tool registry, hook extension boundary, provider tool call shape, tool result projection, trace recorder.
