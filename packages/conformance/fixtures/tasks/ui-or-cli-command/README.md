# ui-or-cli-command

Validates a command-like product entry by routing a deterministic checklist update through the shared todo command atom.

Covered lego planes: UI/CLI command boundary, todo tool, session state, tool result projection, trace recorder.
