# permission-denied

Validates that task-level write permissions block a disallowed file mutation and surface the denial as tool evidence.

Covered lego planes: hook permission policy, write tool, filesystem port, artifact verifier, trace recorder.
