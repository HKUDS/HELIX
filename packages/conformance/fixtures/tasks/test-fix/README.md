# test-fix

Validates the loop of running a failing test, editing the implementation, and re-running the test successfully.

Covered lego planes: runtime process runner, bash tool, edit tool, permission policy, tool result projection, workspace diff verifier.
