# multi-file-refactor

Validates a small cross-file rename where one module export and its consumer must stay consistent.

Covered lego planes: prompt, turn loop, edit tool, filesystem port, permission policy, workspace diff verifier, trace recorder.
