# tool-error-retry

Validates recovery after a deterministic tool error: the first edit misses, the second edit succeeds, and the final output reports recovery.

Covered lego planes: edit tool result shape, sequential tool execution, permission policy, workspace diff verifier, trace recorder.
