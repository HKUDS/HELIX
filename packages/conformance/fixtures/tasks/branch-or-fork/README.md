# branch-or-fork

Validates that the task runner can exercise the session fork API after a turn and verify the forked transcript.

Covered lego planes: session persistence, session branching, transcript projection, trace recorder.
